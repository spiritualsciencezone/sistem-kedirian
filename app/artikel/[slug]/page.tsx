import type { Metadata } from 'next'
import { notFound } from 'next/navigation'
import { SiteHeader } from '@/components/site-header'
import { SiteFooter } from '@/components/site-footer'
import { formatDate, getPublishedBySlug } from '@/lib/content'

export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const article = await getPublishedBySlug('article', slug); return article ? { title: `${article.title} | Sistem Kedirian`, description: article.excerpt } : { title: 'Artikel | Sistem Kedirian' } }
export default async function ArticleDetailPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const article = await getPublishedBySlug('article', slug); if (!article) notFound(); return <><SiteHeader /><main className="pt-16"><header className="border-b border-border/60"><div className="mx-auto max-w-4xl px-6 py-20 sm:py-28"><p className="text-[0.65rem] uppercase tracking-hairline text-primary/80">{article.category}</p><h1 className="mt-6 font-serif text-5xl font-light leading-none text-balance sm:text-7xl">{article.title}</h1><p className="mt-8 max-w-2xl font-serif text-xl font-light leading-relaxed text-muted-foreground">{article.excerpt}</p><p className="mt-8 text-xs uppercase tracking-widest text-muted-foreground">{formatDate(article.published_at)}</p></div></header><article className="mx-auto max-w-3xl px-6 py-16 sm:py-24"><div className="space-y-7 text-base leading-relaxed text-muted-foreground">{article.content.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}</div></article></main><SiteFooter /></> }
